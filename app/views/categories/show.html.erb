<h1><%= @category.name %></h1>

<% @category.books.each do |book| %>
  <b>ID </b><%= book.id %>
  <b>Name </b>
  <%= link_to book.name, book_path(book) %>
  <b>Description </b><%= book.description %><br>
<% end %>

