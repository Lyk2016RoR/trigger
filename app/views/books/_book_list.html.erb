<% @books.each do |book| %>
	<div class="panel panel-default">
		<div class="panel-heading">
			<i class="fa fa-plus"></i>
			<%= link_to book.category.name, category_path(book.category) %>
			<i class="fa fa-angle-right"></i>
			<%= link_to book.name, book_path(book) %>
			<span class="pull-right label label-success">{<%= book.average_rating %>}</span>
		</div>
		<div class="panel-body">
			<p>
				<%= book.description %>
			</p>
		</div>
    	<div class="panel-footer">
			<div class="pull-left">
				<p>
					<span><i class="fa fa-user"></i> <strong>Ekleyen:</strong>
					<a href="#"><%= book.user.email %></a></span>
					<span>
						<i class="fa fa-calendar-o"></i>
						<strong>Eklenme Tarihi:</strong>
						<%=
	book.user.created_at.strftime("%d %B %Y")
						%>
						</span>
		 	   	</p>
		  	</div>
	      	<div class="pull-right">
				<%= link_to book_path(book), class: "btn btn-default" do %>
					Devamını Oku
					<i class="fa fa-angle-right"></i>
				<% end %>
	      	</div>
	      	<div class="clearfix"></div>
    	</div>
	</div>
<% end %>